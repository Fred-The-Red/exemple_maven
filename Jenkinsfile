pipeline {
  agent any
  stages {
    stage('init') {
      steps {
        sh 'echo "hello-world"'
      }
    }
    stages {
    stage('fin') {
      steps {
        sh 'echo "goodbye-world"'
      }
    }
  }
}
